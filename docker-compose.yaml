version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    restart: unless-stopped
    networks:
      - red_ia
    ports:
      - "6333:6333"
    volumes:
      - ./qdrant_storage:/qdrant/storage

networks:
  red_ia:
    external: true
